"""Experiment / analysis scripts exposed for reuse by tests and CLI.

The scripts in this package can still be invoked directly as ``python
scripts/<name>.py`` for CLI-style use; importing them (e.g. from tests)
requires the ``scripts.`` prefix.
"""
