# 主依赖 (运行时)
# cflib 0.1.32 matches the 2026.04 firmware generation and includes supervisor
# port support plus CRTP protocol v11 param MISC compatibility.
cflib>=0.1.32,<0.2
numpy>=1.24,<3
scipy>=1.10
matplotlib>=3.7
PyYAML>=6
cvxpy>=1.4

# Web 前端 (PR-A: 离线分析 Web UI)
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1

# 可选: C++ radio driver 后端 (需要 MSVC Build Tools)
# cflinkcpp
